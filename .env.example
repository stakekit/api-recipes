MNEMONIC=
BORROW_API_KEY=
BORROW_API_URL=https://borrow.yield.xyz
# STOPGAP: the borrow API returns EVM payloads without nonce/gas fees, so the borrow recipe
# populates them client-side before signing. Set an RPC endpoint for the transaction's network.
RPC_URL=
PERPS_API_KEY=
PERPS_API_URL=https://perps.yield.xyz
YIELDS_API_KEY=
YIELDS_API_URL=https://api.yield.xyz
